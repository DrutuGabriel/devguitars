// import {} from '../actions/types';


const siteReducer = (state = {}, action) => {
  switch(action.type){
    default:
      return {
        ...state
      };
    
  }
}

export default siteReducer;